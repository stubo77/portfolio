name = "portfolio"
compatibility_date = "2024-01-01"
pages_build_output_dir = "public"

# KV namespace for user accounts.
# After running `wrangler kv namespace create AUTH_KV`, paste the id below.
[[kv_namespaces]]
binding = "AUTH_KV"
id = "9d7d35fc39994d0d99c4d629c2f268b6"

# JWT_SECRET is set as a secret via:
#   wrangler pages secret put JWT_SECRET
# (do NOT put the actual value here)
